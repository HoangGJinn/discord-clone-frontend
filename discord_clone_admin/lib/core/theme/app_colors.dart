import 'package:flutter/material.dart';

class AppColors {
  // ── Brand ────────────────────────────────────────────────────
  static const Color blurple = Color(0xFF5865F2);
  static const Color blurpleDark = Color(0xFF4752C4);
  static const Color blurpleLight = Color(0xFF7289DA);

  // ── Backgrounds ───────────────────────────────────────────────
  static const Color background = Color(0xFF1E1F22);
  static const Color surface = Color(0xFF2B2D31);
  static const Color cardBg = Color(0xFF313338);
  static const Color navBg = Color(0xFF1A1B1E);
  static const Color inputBackground = Color(0xFF1A1B1E);
  static const Color overlay = Color(0xFF232428);

  // ── Borders ───────────────────────────────────────────────────
  static const Color divider = Color(0xFF3A3D45);
  static const Color border = Color(0xFF404249);

  // ── Text ──────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFFF2F3F5);
  static const Color textSecondary = Color(0xFFB5BAC1);
  static const Color textMuted = Color(0xFF949BA4);
  static const Color textLink = Color(0xFF00A8FC);

  // ── Semantic ──────────────────────────────────────────────────
  static const Color danger = Color(0xFFFA777C);
  static const Color dangerDark = Color(0xFFD9434E);
  static const Color success = Color(0xFF57F287);
  static const Color successDark = Color(0xFF3BA55D);
  static const Color warning = Color(0xFFFEE75C);
  static const Color warningDark = Color(0xFFC9A227);
  static const Color gold = Color(0xFFFFD700);
  static const Color info = Color(0xFF00A8FC);

  // ── Status ────────────────────────────────────────────────────
  static const Color online = Color(0xFF57F287);
  static const Color idle = Color(0xFFFAA81A);
  static const Color dnd = Color(0xFFED4245);
  static const Color offline = Color(0xFF80848E);

  const AppColors._();
}
